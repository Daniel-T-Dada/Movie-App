// import { movieData } from "@/app/data"

// const MovieDetails = ({params}) => {
//     const details = movieData.find(movie => 
//         String(movie.title).toLowerCase().split(' ').join('-') = params.slug
//     )

//     const relatedMovies = movieData.filter((card => card.category == details.category && details.category !== card.title))
//     return (
//         <>

//         </>
//     )
// }
// export default MovieDetails